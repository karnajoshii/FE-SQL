/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // If you had any custom configuration in your next.config.ts, copy it here
}

module.exports = nextConfig